{
  "name": "telegram-mini-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd backend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build": "next build && cd backend && npm run build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "concurrently": "^9.1.0"
  }
}
